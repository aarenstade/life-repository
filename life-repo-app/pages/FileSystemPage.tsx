import { FC } from "react";
import { View } from "react-native";

interface FileSystemPageProps {}

const FileSystemPage: FC<FileSystemPageProps> = () => {
  return <View></View>;
};

export default FileSystemPage;
